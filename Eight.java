package com.example.groupproj_blackjack;

public class Eight extends Card { // Eight class is subclass of Card

    public Eight(Suit suit, int value, String name) { // uses Card suit, value, and name
        super(suit, 8, "Eight"); // calls from superclass Card and implements these values for Eight class
    } // closes constructor Eight
} // closes class Eight
