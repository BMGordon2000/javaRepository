package com.example.groupproj_blackjack;

public enum Suit { // identifies variables for card deck, what each symbol is
        DIAMOND,
        HEART,
        SPADE,
        CLUB
} // closes enum Suit
